[package]
name = "docs-gate"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
regex = "1"
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
